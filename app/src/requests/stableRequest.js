const got = require('got');
const { XMLParser, XMLBuilder, XMLValidator} = require("fast-xml-parser");

var Username = ''
var Password = ''


async function getPolicyNumber(pQuery) {
    return pQuery
}


module.exports = {getPolicyNumber}